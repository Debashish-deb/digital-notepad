[pytest]
pythonpath = .
testpaths = tests
markers =
    requires_database: needs TEST_DATABASE_URL or local POSTGRES_CONN (runtime skip)
    requires_production_db: needs OMEIA_ALLOW_PRODUCTION_DB_TESTS=1
